@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "List of Employee";
}
@model IEnumerable<Employee>

<h1 class="bg-info text-black">All Employees</h1>
<a asp-action="Create" class="btn btn-secondary">Add New Employee</a>
<table class="table table-sm table-bordered">
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Designation</th>
        <th>Department Name</th>
        <th>Edit</th>
        <th>Delete</th>
    </tr>
    @foreach (Employee employee in Model)
    {
        <tr>
            <td>@employee.id</td>
            <td>@employee.name</td>
            <td>@employee.designation</td>
            <td>@employee.Department.name</td>
            
            <td>
                <a class ="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@employee.id">Edit</a>
            </td>
            <td>
                <form asp-action="Delete" asp-route-id="@employee.id" method="post">
                    <button type="submit" class="btn btn-danger btn-sm">Remove</button>
                </form>

            </td>
        </tr>

    }
</table>
